{
  "name": "DockerPresentation",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "start": "node server/server.js --debug",
    "docker": "gulp; server/server.js --debug",
    "postinstall": "bower install"
  },
  "dependencies": {
    "express": "^4.12.4"
  },
  "devDependencies": {
    "gulp": "^3.8.11",
    "gulp-jscs": "^1.6.0",
    "gulp-jshint": "^1.10.0"
  }
}
